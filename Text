#include <iostream>
#include <string>

using namespace std;

int main()
{
    string text;
    cout << "Enter text to be read: ";
    getline(cin, text);
    
    for (int i = 0; i < text.length(); i++)
    {
        cout << text[i];
        system("say " + text[i]);
    }
    
    return 0;
}

